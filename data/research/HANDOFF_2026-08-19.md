# Handoff — NJ Black Press (updated 2026-08-19, end of session, clean stopping point)

Read data/research/SESSION_PLAN_2026-08-20.md for the full task table. This file is the fast resume.

## Where things stand

Milestones 1–6 are DONE and pushed. origin/master = local master = ffa1f64. No agents in flight. Tracked tree is clean.

Session commits, in order: 7e5143d (type/slop sweep + wiki regen), 60bba17 (55 clippings + clippings.json, closed #54), f1c3550 (tap targets, closed #56), deaf944 (alt text, closed #43), 1393c36 + ec5de47 (fact-check: 102 defects fixed, id 24 merged into id 9, 136 publications, closed #42), df3b6fe (usability pass, closed #44), ffa1f64 (Woven experience, closed #50–53).

The site now has: 136 publications, evidence sections on publication pages (images only from docs/data/clippings.json — 55 entries, all with alt text), nav search, mobile event buttons, labeled filters, a regenerated wiki with main-site backlinks, and Woven (docs/woven.html — 136 loom threads, 97-ghost cloth, story tours, DOM twin at ?nogl=1, vendored three.js r171). Joe approved the Woven look. The resize regression is fixed and browser-verified at 1440 and 375 (zero console errors, no overflow).

#47 updates posted: discussioncomment-18084394 (fact-check/merge/usability) and -18084395 (Woven).

## Open items for the next session

1. Joe owes a font pick from the type specimen (candidates: Libre Franklin [provisionally applied], Archivo, Oswald, Barlow Condensed, League Gothic). docs/type-specimen.html is untracked and must NEVER be committed; delete it after the pick. A preview server may still be running on port 8777.
2. Deferred polish (director ruling s21): apply the interfaces.dev cheat-sheet items to index/archive (tabular-nums counters, text-wrap balance, hover-media gating, focus-visible on nav search/chips, role=status on results count). The publication pages already have them.
3. Small cleanups: 10 clippings captions end in raw file stems; story-013 citation repeats its caption.
4. SFTP deploy to the live server (see CLAUDE.md) — GitHub Pages is stale by design.
5. Repo-public gate unchanged: safety audit, rights-binary handling, Joe's final word.

## Hard rules in force

- Director mode: subagents do the work; verify every report against disk; adversarial verify for discoveries; sentence case; plain-English #47 updates per milestone.
- Strict ASD-STE100 in all director communication (replies, plan, handoff, briefs, #47). Does not apply to site editorial text. Joe can override.
- Commit email MUST be 6799804+jamditis@users.noreply.github.com (repo-local config set; GH007 blocks montclair.edu). The pre-commit hook blocks AI-attribution trailers — commit without them.
- NO destructive git commands by agents. NO blanket process kills — an agent kills only the exact PID it started.
- .chrome-cdp/, archival research media, *.backup-premerge24.json, and depth-hunt/apex_titles.json never enter git.
- Only images in docs/data/clippings.json may render on the site. Never render evidence `file` paths. Standing exclusions: Right On! CA-era, six out-of-state clippings, NJ-only keepers.
- Never hand-edit docs/wiki/ or okf/ — fix scripts/generate_html_wiki.py and regenerate.
- Do NOT re-run convert_csv.py (it wipes enriched JSON fields).

## Other workstreams warning

origin receives pushes from Joe (CLI + web), Cassandra, Claude web sessions, and Codex PRs. Fetch before pushing; merge, never rebase shared history. publications.json activity fields are remote-canonical; evidence arrays are regenerated by data/add_evidence.py.
