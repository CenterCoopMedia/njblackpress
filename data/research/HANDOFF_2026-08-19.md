# Handoff — NJ Black Press (updated 2026-08-19, second session, pre-compaction)

Read data/research/SESSION_PLAN_2026-08-20.md for the full task table. This file is the fast resume.

## Where things stand

Milestones 1–4 are DONE, closed on GitHub, and pushed. origin/master = local master = 425b831 (before the in-flight sweep commits). Live site is deploying from that push: wood-and-fabric palette, upgraded decade timeline, editorial data (81 events, 13 stories), rights manifest (327 files), evidence arrays (181 entries, 137 pubs, activeCount 17). All data tests pass: test_source_catalog.py, test_evidence.py, test_site_data.py.

## Agents in flight at compaction

1. Type-and-slop sweep (opus): removing Fraunces entirely (Joe's ruling), choosing a wood-type condensed grotesque display face (Big Shoulders front-runner), removing eyebrow text / italic hero titles / status pills / amber-emerald off-palette per data/research/design/AI_SLOP_PROHIBITIONS.md, verifying at desktop + 375px. Instructed NOT to commit. When it reports: verify on disk (node --check, grep for Fraunces leftovers, screenshots claims), commit explicit paths, push (email is fixed — see below), post #47 update.
2. Clip production (#54, sonnet): producing docs/images/evidence/ + docs/data/clippings.json from the rights manifest crop plans. Very long-running; was pinged for status with no reply yet. If still silent, TaskStop and relaunch from its log (data/research or scratchpad); its outputs are untracked and safe.

## Next actions in order

1. Land the sweep (verify → commit → push → #47 update).
2. Mobile audit + fixes, issue #56 (375px/768px, Playwright, touch targets) — mobile is a launch gate.
3. Woven build (#50–53) from data/research/design/WOVEN_SPEC.md: vendored three.js r171 into docs/vendor/, era-band thread routing, 19-active rule, ghost count must be reconciled (spec says 98, issue #52 says 93), tours play citation-only until clippings.json exists. Build order tickets are in the spec.
4. #48 remainders: full type sweep pass 3/4, border-white/10 → walnut hairline sweep, JS-rendered card styling.
5. Milestone 6 (#42–44): fact-check every claim against source images (include deferred nits: "Newark weeklies" plural outside thread 10, memo line 99 wording, ids 9/24 duplication question — id 60 is resolved, remapped to 126), alt text for every clipping, usability review including a phone pass.
6. Repo-public gate: unchanged, see original handoff terms (safety audit, rights binaries, Joe's final word).

## Hard rules in force

- Director mode: subagents do the work; verify every report against disk; adversarial verify for discoveries; sentence case; plain-English #47 updates per milestone (an achievement is not done until posted).
- NO destructive git commands by agents (a reset --hard wiped finished work this session; it was rebuilt). Protect the tree before any recovery operation.
- Commit email MUST be 6799804+jamditis@users.noreply.github.com (repo-local config set; montclair.edu is blocked by GitHub GH007; the unpushed range was history-rewritten once already to fix this).
- .chrome-cdp/ and research media never enter git (.gitignore covers it).
- AI slop design tells prohibited globally (data/research/design/AI_SLOP_PROHIBITIONS.md): no eyebrow text, no italic heroes, no status pills, no Fraunces/serif display, plus the full researched list. Applies to Woven too.
- Every page fully responsive and mobile friendly (issue #56); Woven needs a mobile mode + DOM twin.
- Standing exclusions: Right On! CA-era issues, six out-of-state clippings, NJ-only keepers.

## Joe's creative directives (this session)

- "Wood and fabric" design feel — implemented as the material language in DESIGN_DIRECTION.md (walnut/linen/oak/thread tokens, stain accent #e2662b, CSS textures, reading-room idea).
- Three.js "big and ambitious, culturally meaningful" → "Woven": 138 publications as loom threads, evidence-weight thickness, 13 story camera tours, ghost cloth memorial for the ~93–98 catalog-only papers, full a11y twin. Spec: WOVEN_SPEC.md.
- Full GitHub tracking: issues #48–56 on milestone 5 + project 3. Keep it current.

## Other workstreams warning

origin receives pushes from Joe (CLI + web), Cassandra, Claude web sessions, and Codex PRs. Fetch before pushing; merge (never rebase shared history); publications.json activity fields are remote-canonical, evidence arrays are regenerated by data/add_evidence.py.
